services:
  beacon:
    build: .
    ports:
      - "8000:8000"
    environment:
      BEACON_DATA_DIR: /data
      BEACON_BASE_URL: ${BEACON_BASE_URL:-http://localhost:8000}
    volumes:
      - ./data:/data
